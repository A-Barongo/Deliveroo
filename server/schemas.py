from marshmallow import Schema, fields
class ParcelSchema(Schema):
    id = fields.Integer(dump_only=True)
    description = fields.String()
    weight = fields.Float()
    status = fields.String(dump_only=True)
    sender_name = fields.String()
    sender_phone_number = fields.String()
    pickup_location_text = fields.String()
    destination_location_text = fields.String()
    pick_up_longitude = fields.Float()
    pick_up_latitude = fields.Float()
    destination_longitude = fields.Float()
    destination_latitude = fields.Float()
    current_location_longitude = fields.Float(allow_none=True)
    current_location_latitude = fields.Float(allow_none=True)
    distance = fields.Float(allow_none=True)
    cost = fields.Float(dump_only=True)
    created_at = fields.DateTime(dump_only=True)
    updated_at = fields.DateTime(dump_only=True)
    recipient_name = fields.String()
    recipient_phone_number = fields.String()
    courier_id = fields.Integer(allow_none=True)
    user_id = fields.Integer(dump_only=True) 